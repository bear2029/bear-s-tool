jest.dontMock('promise');
jest.dontMock('../subscription');
var subscription = require('../subscription');
var _ = require('underscore');
xdescribe('summaryHilight', function() {
	var hilights,term;
	beforeEach(function(){
	})
	it('should return not null',function(){
		expect(hilights).not.toBeNull();
	})
})
